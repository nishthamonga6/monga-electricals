"use client";
export default function WhatsAppButton() {
  // Kept for compatibility (FloatingActions now provides the visible button). This component is visually hidden.
  return <div className="sr-only" aria-hidden />;
}
